export const contactConfig = {
  whatsappNumber: '201147372440',
  whatsappMessage:
    'Assalamu alaikum, I would like to learn more about Daris programs.',
  // Public-facing inbox shown in the footer + on Contact / About / FAQ /
  // Programs as a mailto: link. Must be a real, monitored address —
  // anything that bounces (e.g. a placeholder example domain) leaves
  // first-time visitors with no way to reach anyone. If this domain
  // doesn't yet have email receiving set up, configure forwarding at
  // the registrar or update this constant to the sheikh's preferred
  // address.
  contactEmail: 'contact@daris.education',
  // Optional: Formspree or similar endpoint. Leave empty to use mailto.
  formEndpoint: 'https://formspree.io/f/xnjbwqnj'
};

